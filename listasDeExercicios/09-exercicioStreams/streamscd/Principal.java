
/*
public List<CD> cdsFavoritos(List<CD> cds) {
  List<CD> favs = new ArrayList<>();

  for (CD cd: cds) {
    boolean temFavorita = false;
    for (Musica m: cd.getMusicas()) {
      if (m.getEstrelas() >= 4) {
        temFavorita = true;
        break;
      }
    }
    if (temFavorita) {
      favs.add(cd);
    }
  }
  return favs;
}
*/